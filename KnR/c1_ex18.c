#include <stdio.h>

#define MAXLINE

int myGetline(char line[], int maxline);
void copy(char to[], charfrom[]);


int main(void){

	int c, i;
	int line[MAXLINE + 1];
	
	c = getchar();
	for(i = 0; i < lim

}
