#include <stdio.h>
#include <string.h>

int main(void){

enum months {Jan = 1, Feb, Mar, Apr, May, Jun, Jul, Aug, Sep, Oct, Nov, Dec};
enum months total;
	total = 17 + Feb;

	printf("total  value = %d:",total);

}

