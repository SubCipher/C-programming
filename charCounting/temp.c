#include <stdio.h>
/* count new lines  */

main(){

	int inputText;
	while ((inputText=getchar()) != EOF){
		putchar(inputText);
		if ('	' == putchar(inputText)){

		printf("new string %d",putchar(inputText));
		}	
}

}
